/* Generated by TestGen on Tue Oct 3 2023 at 6:14 AM (GMT) */
@isTest
private class AccountTrigger_TGN_TEST{
    @isTest
    private static void run(){
        Database.SaveResult dsr;
        Contact drContact;
        Account drAccount;
        drAccount=new Account(ContactEmails__c='a');
        dsr=Database.insert(drAccount,false);
        Database.update(drAccount,false);
        drContact=new Contact();
        Database.insert(drContact,false);
    }
}